services:
  - type: web
    name: roadrisk-peru
    env: python
    buildCommand: "pip install -r requirements.txt && python -m src.roadrisk.train"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    plan: free

